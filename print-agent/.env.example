# 服务端地址
SERVER_URL=http://localhost:3000
# PC代理密钥（与服务端 PC_AGENT_SECRET 一致）
AGENT_SECRET=agent-dev-secret
# 代理唯一标识（多打印机时每台一个不同的ID）
AGENT_ID=agent-win-001
# 打印机名称（Windows 打印机的确切名称）
PRINTER_NAME=Microsoft Print to PDF
# 打印机类型: bw=黑白, color=彩色, photo=照片
PRINTER_TYPE=bw
# 轮询间隔（秒）
POLL_INTERVAL=5
